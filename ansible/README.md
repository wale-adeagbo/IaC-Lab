# Ansible — Configuration Management

> **Status: Coming Soon**

This section will cover Ansible playbooks for automating configuration management on top of the Kubernetes and Terraform infrastructure already built in this lab.

## Planned Projects

- Install and configure MicroK8s via Ansible playbook
- Deploy Kubernetes manifests using the Ansible `kubernetes.core` collection
- Combine Terraform (provisioning) + Ansible (configuration) in a single workflow

---

*Check back as this lab progresses.*
